import styled from "styled-components";

const StyledStartBanner = styled.div`
  img {
    width: 15rem;
  }
`;

const StyledStartContent = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 3rem;
`;

export { StyledStartBanner, StyledStartContent };
